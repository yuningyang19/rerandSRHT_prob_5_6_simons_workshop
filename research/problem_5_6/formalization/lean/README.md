# Problem 5.6 Lean workspace

This is the project-local Lean 4/Mathlib workspace for the three-stage
formalization of `research/problem_5_6/LaTeX/solution.tex`.

- Lean: `v4.33.0`
- Mathlib tag: `v4.33.0`
- Mathlib commit: `db584cd6d46c92f209a44c0f1c829460d327499d`
- Operation: `PROBE`
- Current boundary: Gate 3 proof formalization complete (53/53 declarations)

The corrected public mathematical signatures are bound by
`../corrected_signature_manifest_v2.json` after the source-faithful extraction
repairs recorded in `../source_to_lean_correction_report_20260905.md`.  The old
Gate 1/2 files remain historical records.

A compiling declaration containing `sorry` would be signature scaffolding, not
proof coverage.  Gate 3 now has no such declaration: all 53 declarations, all
42 interfaces, and all nine named public results are transitively
kernel-closed.  The declaration surface builds through
`Problem56.Dependencies`; `../dependency_manifest.json` remains the planned
mathematical DAG only.  Actual proof-term and axiom evidence is generated by
`Problem56/DependencyAudit.lean` and summarized in
`../actual_dependency_axiom_audit_20260905.md`.  Final counts and the closed
load-bearing chain are recorded in `../gate3_obligation_ledger.md`.

The final I20 closure is in `ContractedCoreEncodingClosure`, backed by
`ContractedCoreCanonicalRealization`, `ContractedCoreCanonicalCard`, and the
separate `ContractedCoreZeroParameter` classification.  Other reusable no-hole
modules include `GraphOperatorL2`, `GraphEven`, `SpectralRoot`,
`ProbabilityFrobenius`, `SamplingArithmetic`, `TwoProjection`,
`CumulantMoment`, `SelectorQuotient`, `EqualityBudget`, and `LargeRankCutoff`.
Definitions-only extraction witnesses are in `ExtractionRegression`; small
graph representation checks, including an exact unequal-dimensional chain,
are in the two `GraphOperator*Regression` modules.
