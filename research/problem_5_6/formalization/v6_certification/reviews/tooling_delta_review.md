# Independent tooling delta review

Reviewer `/root/baseline_audit`; hashes and exact fixture results are in `tooling_delta_review.json`. This updates the earlier bounded tooling review without overwriting its historical snapshot.

The new lexical source policy strips nested block comments, line comments and ordinary strings before rejecting holes, local axioms, unsafe/native proof escapes and unchecked declaration insertion. It now catches anonymous example holes as well as named theorem issues. This is a structural policy check; the checked-environment dependency traversal and stored-proof replay remain the substantive proof checks. The current imported local source closure passes this policy. All 19 current isolated negative fixtures reject.

Frozen named/interface/definition inventory hashes and 12/50/34 denominators are now checked, as are crosswalk row identity/uniqueness, source context bytes and supplied theorem type hashes. Two binding findings remain at the recorded checker hash: a claimed-closed row can omit its actual type entries, and actual definition records are not compared by that row validator. Frozen B/C manifest contents also need a fixed reviewed hash or an equivalent immutable source anchor, rather than trusting editable manifest fields alone. The controller acknowledged these findings and is adding nonempty covered-row checks, definition record validation and a final reviewed crosswalk hash lock. No PASS for the future fixes is inferred before inspecting them.

The four prior empty non-open crosswalk rows have concrete existing locators in the 189-target graph. I08 uses bernoulli_coordinate_sampling_corollary_of_trace plus its closed client; I23 uses signBernoulliExpectation_trace_centered_product_pow_expansion and trace_centeredSelector_product_pow_expansion; I24 uses signBernoulliExpectation_trace_eq_partition_cumulant_sum with the actual functionPartitionEquiv and singleton-selector vanishing helpers; I40 uses signedTrace_master_geometric_bound with the actual finite-majorization helpers. Exact names, source lines and qualifications are in JSON. The last public root has a later geometric majorant, so its private finite helper types/body must stay attached to the literal finite-interface mapping.

The pinned Lean recursor definition exposes reduction-rule RHS expressions, constructor names and the associated inductive names. Pinned collectAxioms visits only the recursor type, as did the initial auditor. That omission is not a demonstrated safety failure: accepted recursors are kernel generated, and the required stored-proof replay checks the imported objects. Including rule RHS constants and constructor/inductive references is appropriate for the stronger claim of a full explicit dependency graph. The resulting node count and unchanged-or-expanded axiom set must be recorded, not silently substituted into an old graph receipt.

The actual installed Lake help confirms that --no-cache disables downloading build caches. The pinned Mathlib lakefile has a separate post_update cache hook; run_suite.py now sets MATHLIB_NO_CACHE_ON_UPDATE=1, while mandatory build commands use lake --no-cache build. Both changes are necessary and correctly directed. Neither flag proves cache absence: the final independent source-cold run must begin with no .lake and must preserve evidence of source compilation, with no copied objects or cache downloads.

record_environment.py checks the exact manifest dependency revisions and rejects modified tracked dependency sources, records Lean/Lake/Python versions, and requires the pinned Lean 4.33.0 version. Its trust boundary is the standard installed distribution. It does not authenticate toolchain binaries or establish that a particular checkout started empty. Fresh execution receipts must follow the final script/source freeze.

Subsequent inspected repairs: exact A/B/C source-manifest SHA256 pins are now enforced, and AuditTools traverses recursor rule RHS/constants/constructors/associated inductives. Numeric private names are parsed with Name.num components and retain the same checked-environment, declaration-kind and axiom gates. Updated hashes are in the JSON followup section. Final crosswalk nonempty/definition/hash-lock validation and refreshed complete graph execution remain pending at this report.
