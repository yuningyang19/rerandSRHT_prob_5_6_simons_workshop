# Bounded source-first written-proof review

Reviewer: `/root/v6_inventory`. Separate from authors of all new proof modules.
Frozen manuscript: `4551d08f3732a3ca06c2f576794e1d75790e1972`.

`SOURCE_FIRST_WRITTEN_PROOF_REVIEW: COMPLETED_BOUNDED_PASS`

`FINAL_TWO_PASS_LEAN_TO_MANUSCRIPT_REVERSE_AUDIT: PENDING_IMPLEMENTATION`

This report completes a full reading of the written C proof against A and a type-level/selected-body comparison with B. It does not stand in for the required final independent second reverse pass from the completed combined Lean closure. This distinction preserves independence and avoids approving code that has not yet been produced.

The machine-readable `three_way_candidate_map.json` links all 12 named results, 50 interfaces and 34 definitions to the exact source inventory. Its 96 entries preserve expected scopes and distinguish candidate names from the 53 baseline declarations for which the actual fully elaborated types are recorded. Candidate A regions are proof-region pointers, not automatic proof-port acceptance. Every actual-type pointer has the source manifest hash and exact expression/printed-type hashes. The compiled C auxiliary file reconciles named results as Theorem 1, Lemmas 2/4/5/6/7/8/9/11/12, Propositions 3/10, and the seven definitions 3.1–3.5/4.1–4.2. No historical nine-result denominator is used.

## Written mathematical checks

| Frozen C interfaces | Comparison and result |
|---|---|
| 01–03 | Exact Walsh/sign order and Gram normalization agree with A. Fixed-frame OSE interpretation needs the independently frozen client proof. Cyclic trace equality is a standard rectangular trace identity with positive power. |
| 04–07 | C expands A's entire finite two-projection argument. The interior w vector is normalized because norm(Eu)^2=lambda; distinct pairs, endpoints and the ker-P complement are accounted for. The determinant is positive a^2; defective-root trace equality uses Cayley–Hamilton, which B actually implements. Nonreal roots have modulus a. The 63/64 and 55/64 arithmetic and the endpoint lower bounds are valid. No expectation-of-absolute-trace assumption is inserted. |
| 08 | Taking expectations of the compensated signed trace, substituting delta theta=r theta^2/kappa, and using the chosen logarithmic p yields 1/1000 with the stated K0. B named type has the same probability and parameters. |
| 09–13 | C's finite graph sums and rank-edge factorization are correct local algebra with rectangular dimensions. Loops contribute twice to degrees; inserted r-dimensional vertices have degree two even for a distinguished loop. B implements a positive-even Euler-based conversion, a valid alternative to the source's broader bridgeless conversion at the actual rank-contraction use. The general prose claim and boundary-entry-preservation claim require separate correspondence rather than identifying them with I05. |
| 14–16 | The formal squarefree-coefficient logarithm and exponential argument is valid with only finite requisite moments. Ordered block permutations account for j! and the cancellation coefficient. Independence means vector/group independence. The logarithm identity is formal: no analytic moment-generating convergence is claimed. B finite uniform identities need the genuine measure extension; the newly frozen six expected Props preserve this. |
| 17–22 | Four sign occurrences per entry remain distinct positions. Connected join gives connected quotient paths; middle degree is block size and input degree is twice block size. Different cumulant blocks may share labels, so contraction is unrestricted signed, and the absolute value belongs outside each graph sum. Pair count and two coefficient products give exponent 12q. Character conjugation preserves both independent sign laws at the same V, and the conditional diagonal averages yield E P=delta I. |
| 23–26 | Word-to-equality-partition regrouping requires injective full Walsh block labels. Degrees are 2m_u and excess 4s. Selector expectation factors into ordinary moments and singleton blocks vanish; it is not a selector cumulant calculation. The centered Bernoulli bound and theta^v coefficient hold. |
| 27–30 | Marked degree bounds, the degree-four local classification and terminal-path arguments are consistent. With B nonempty the returning paths retain two separate ports. The 1+8+18+36+9=72 exponent counts q/degrees/edge pairing/ports/lengths; Euler transitions add 2s≤2a. Starting directions include two half-edges of loops. Renaming auxiliary vertices does not add a separate permutation factor. The a=0 p=2 parallel-four-edge graph is the degenerate doubled-cycle case, and both p=2 examples are consistent. The p=7 example has multiplicities 5,3,2,2,2 and decodes the displayed 14 positions exactly. |
| 31–34 | Entry partition rho and selector partition pi have distinct roles. Singleton entry loops vanish by zero diagonal mean. Two forbidden pair types are removed before kernel counting. Unrestricted cardinality is n^(v−h), with h actual rank; injective solutions form a subset. Large blocks use ≤6d occurrences and ≤12d vertices. Every remaining odd vertex lies in a disjoint-pair row, and a basis of weight-four rows gives support union≤4h. B's broader weight-four row selector is a safe superset, not identical to the source U. |
| 35–39 | Exact weight c(rho) matches B's natural finite sum. Large-block count/weights contribute 12d+72d, disjoint pairs 12h+2s, loops 2t+6s+1, parallel groups s, totaling the stated exponent. Empty or incompatible classes contribute zero. The per-fixed-d,h absolute row-label/selector bound must be exposed separately; a total aggregate norm bound is not itself the named C proposition. The dimension identity is valid when nonempty classes give d<p,h≤p−s; all other cases need a zero-class proof. |
| 40–41 | Feasible ranges retain both vertex and cumulant block restrictions. Enlarging the t-range is legitimate only for nonnegative bounds, and the 912d+304h contribution leads to 83s+996d+316h. The triple geometric series uses rank≥2L^1000. The final 128pL≤640p^2≤64^p holds at p=2 and by the stated growth comparison. |
| 42–45 | The paper's continuous uniform common-order coupling is a valid ordinary probability proof: no ties almost surely, symmetry gives a uniform k-subset independent of X, and count-bracketing yields set inclusion. The two Gram events need only marginal bounds; union bounding does not require their independence. The spectral interval (1±epsilon/4)^2 lies in 1±epsilon. Binomial mgf optimizations and the denominator 48 agree with the final bound. B uses a finite three-category/order construction, which is an alternative proof only at the frame-law scope it actually quantifies. General X averaging/independence remains an additive obligation. |
| 46–48 | Conditional inclusion probabilities produce (n−k)/(k(n−1)) times (n sum rownorm^4−r); n=1/full sampling handled separately. The Rademacher fourth-moment identity subtracts the twice-overcounted all-equal pattern, yielding r^2+2r−2 sum rownorm^4. Norm domination/Markov produces the claimed small-rank bound and 0.01. |
| 49–50 | R0 and C are exact universal constants. The logarithmic rank cutoff uses x≥20000, log 2>1/2 and monotonic f. Large-width cap arithmetic, theta±≤5/16, effective width≥1536K0r/epsilon^2, and final exp tail comparison are consistent. All three k choices precede V. |

## External graph-source check and remaining boundary

The primary [Mingo–Speicher arXiv source](https://arxiv.org/pdf/0909.4277) was opened and its input-output operator theorem, graph modification definitions and two-edge-connected conversion proof were read. The retrieved 20-page preprint labels the conversion Lemma 15, modifications Definition 12/Proposition 13, and operator bound Theorem 11. It supports arbitrary connected bridgeless conversion and compatible vertex dimensions; C cites the published version with later numbering. The publisher's exact numbered PDF was not obtained in this bounded pass, so C's journal-specific Lemma 16/Definition 13/Proposition 14 page mapping remains to verify. This is a version-locator limitation, not a discovered false mathematical extension. [Published article identity](https://www.sciencedirect.com/science/article/pii/S002212361100440X).

## Necessary repairs and pending formal correspondence

`NECESSARY_MANUSCRIPT_REPAIR_ESTABLISHED_IN_THIS_PASS: NONE`

This is a bounded finding, not final no-repairs certification. Broad graph source numbering, general probability bridges, the graph transpose/boundary bridge, exact binary clauses and fixed-class weighted contribution, and final independent implemented-type checks remain open. The preceding ten scope findings in `expected_statement_review.md` remain controlling.

Reviewer-owned `GraphAndEntryExpected.lean` has SHA256 `1e0a265df84ba4d117cfc8212db8f7b75ecf5ae31e5efa07586ed7b6c25edfb9`. The exact command `lake env lean Problem56/PaperV6/GraphAndEntryExpected.lean`, in the pinned Lean package, returned exit 0. It contains nine intended propositions and their finite-sum definitions, no proof theorem, axiom or hole. Its wider zero-dimensional graph cases are harmless stronger coverage; the named paper has positive coordinate ranges. This reviewer approves these exact expected propositions for proof generation and has not authored their implementations.

No paper edits, B edits, formal proof generation, publication approval or independent final proof ACCEPT occurred in this review.
