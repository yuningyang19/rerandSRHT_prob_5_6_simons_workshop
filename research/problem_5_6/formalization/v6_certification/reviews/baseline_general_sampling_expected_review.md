# Independent general sampling expected statement review

Reviewer `/root/baseline_audit` wrote these intended types from frozen C `lem:sampling` (main.tex:2739–2769) before the root writes the proof. This reviewer is not the proof author. File `Problem56/PaperV6/GeneralSamplingExpected.lean`, SHA256 `f7cfcb4cb64fa8e1260b2754b536a76abfa66eb4acc61d4354990c9cb4aba8c6`. Definitions-only elaboration passed via `lake env lean Problem56/PaperV6/GeneralSamplingExpected.lean`, exit 0.

**APPROVED intended scope.** `GeneralSamplingExpected` uses an arbitrary Ω with probability measure μ, a measurable real finite-dimensional frame X, and a.e. orthonormality. The coordinate Pi map expresses standard matrix-valued random-variable measurability without injecting any Matrix measurable-space instance. A.e. orthonormality includes the ordinary everywhere-frame case and does not impose a distributional restriction.

Both Bernoulli hypotheses are integrals of the literal finite conditional B probability under μ. The conclusion is the same integral for the uniform k-subset law. This exactly models sampling independent of X. It does not demand a pointwise bound by γ or replace averaged probabilities by a maximum. The two densities and final bound are C's exact constants; k≥1 and k≤card α exclude empty sample laws. Neither a Bernoulli nor fixed-size desired bound is built into any definition except as the explicit antecedents and conclusion of the expected proposition.

`FiniteNoiseFailureLawExpected` separately requires independence and the finite noise marginal, then concludes equality between a joint event probability and its iterated conditional finite sum. It permits arbitrary underlying Ω and arbitrary joint realizations of X,S. Specializations must prove measurable Gram-failure sets, instantiate Bernoulli weights or uniform subset weights, and show normalization and marginal values. No independence between Eminus and Eplus is imposed. Those specializations remain actual proof obligations: proving only the abstract integral transfer does not certify the arbitrary independent-joint-law prose without them.

The scalar q nonnegativity and sum-one assumptions in the generic transport express a probability mass function, not any theorem conclusion. For the two concrete sampling laws these are derived facts. All definitions are Prop or real-valued finite sums/integrals; no axiom, desired-bound certificate, or altered B definition was added.
