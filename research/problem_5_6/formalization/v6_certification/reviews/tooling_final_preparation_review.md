# Independent final tooling preparation review

Current tooling delta accepted; final compiler/source receipts pending. The JSON companion pins reviewed bytes. All 23 negative fixtures reject, and the positive constructor-only propext-excess fixture passes.

The structural traversal follows every constructor and recursor rule and therefore can contain axioms not used by a particular target proof term. Requiring collectAxioms to be contained in this conservative closure is correct; equality was too strong. Both sets independently remain within the exact standard three-axiom allowlist, and all extra edges and per-target set differences remain visible. Missing-node and target-node checks are unchanged.

The earlier binding gaps have been closed: immutable source/inventory pins, exact command and cwd, complete per-command source membership, nonempty covered-type locators, raw definition comparisons, and an exact four-file independent correspondence lock. The archive tester checks full membership and hashes, forbids compiled artifacts and caches, follows the documented entrypoint, and fails on changed or missing proof/build inputs.

These Python fixtures check the verifier, not theorems. Real final source-bound compiler replay and a genuinely fresh unpacked no-cache suite remain separate requirements. The installed standard Lean toolchain is the stated trust boundary.

## Final delta and exercised scope rejection

All 25 negative fixtures reject. The new full-paper gate checks reviewed bindings before testing source coverage; an independent real invocation correctly exits 1 for I-V6-11. The encoded-proof verifier reports full manuscript scope NOT_ESTABLISHED. The source-manifest generator was also repaired after the complete-membership check rejected its duplicated lakefile.lean entry: 159 unique source/build files now pass exact membership and hashes. This bookkeeping fix changes no mathematical source, target type, definition value or graph. Final reviewed script hashes and bindings are in the JSON companion.
