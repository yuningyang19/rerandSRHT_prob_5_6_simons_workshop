# Independent forward correspondence review

Reviewer: `/root/v6_inventory`, separate from the historical B proof authors and all current additive proof authors. This reviewer authored only definitions-only expected GraphAndEntry propositions, independently before implementation. Those contain no proof result. Controller preparation packet is not a schema-validated verification-kernel packet; no counterfeit kernel ACCEPT is issued here.

Current status: INDEPENDENT_EXPECTED_AND_ACTUAL_CORRESPONDENCE_BOUND. The current combined manifest has 189 theorem targets and 39 actual expected/primitive definition records, and the 33 reviewer-owned ExpectedChecks examples compiled with exit 0. The full general bridgeless construction remains open. This is a current correspondence report, not a whole-C literal kernel certificate.

The source scope is frozen C commit 4551d08f3732a3ca06c2f576794e1d75790e1972, main SHA-256 3f140ac21cf98d07371ba81c307db1b272251fabe80fd58f7f290683e8d95286, together with its transitive p7 figure. Scope remains 12 named statements / 30 conjuncts, 50 proof-strength interfaces, 34 definitions including 7 named definitions. It is not the historical 9-result or 42-interface scope. All source cells are bound in the frozen inventories; three_way_crosswalk.md exposes the correspondence without replacing those scopes.

## General probability and cumulants

Actual FixedFrame, GeneralCumulantsMoment/Product/Multilinear/Mixed/Shifts/Symmetry, GeneralSampling, FiniteNoiseLaw and SamplingPointwise source bodies were read completely. GeneralMoment/Product public propositions quantify arbitrary measurable probability spaces and integrable products indexed by finite occurrence sets, allowing repeated random variables at different positions. They do not reduce the underlying sample space to a finite type. The algebraic moment and product proofs in fact work with arbitrary moment values; the public finite-moment hypotheses correctly delimit the probabilistic interpretation. The product proof expands and regroups partitions, then uses proved Stirling cancellation. It does not assume a connected-sum identity as a certificate.

Mixed independence is vector/group independence. The proof composes the independent families with measurable finite products, obtains the two-family outer cumulant zero, applies the product identity and isolates the indiscrete partition. Every proper connected partition has a mixed block, and the recursive call strictly decreases the number of factor positions. There is no circular appeal to the same-order mixed-vanishing theorem. Multilinearity isolates the unique partition block containing the changed index and uses finite-integral linearity. Constant-argument vanishing and deterministic shifts retain q>=2 and prove required moment preservation. Odd symmetry uses IdentDistrib of Z and -Z and integrals of powers, with the odd order forcing the sign -1. None uses Gaussianity or analytic moment-generating convergence.

The fixed-frame client obtains a bounded-above witness for the real supremum, inserts the supplied frame, and derives the literal per-frame probability bound for the same C and k. The quadratic-form bridge uses the actual SRHT Gram, V isometry and the symmetric operator-norm characterization. Its event is simultaneous in every vector of one fixed frame. Complement probabilities are valid because k<=n supplies a nonempty exact k-subset space. No all-subspaces-in-one-draw quantifier is introduced.

SamplingPointwise reuses the proved finite common-order coupling, keeps both Bernoulli error marginals separate, and obtains a pointwise failure inequality for every matrix X(omega), without requiring finite Omega. GeneralSampling proves measurability and bounded-event integrability, integrates that inequality, and retains the exact 2 gamma + 2 exp(-epsilon^2 k/48) bound. Its unused orthonormal assumption is harmless stronger actual mathematics: Gram success plus the Loewner sandwich suffices for arbitrary matrices. FiniteNoiseLaw proves the generic independence-to-joint-law equality by disjoint singleton intersections and finite integration. The concrete GeneralSamplingJoint client was independently expected-approved before implementation and its actual body has now been read completely. fixed_noise_failure_law supplies subset nonemptiness, exact uniform-weight normalization and measurable Gram events; bernoulli_noise_failure_law supplies nonnegative product weights and normalization from the mgf at zero. The public joint theorem transports both Bernoulli hypotheses and the fixed-subset conclusion through these actual laws, then applies general_sampling. Thus the source-level arbitrary independently realized sampling application gap is closed, with its current compiled expected-type assignment and actual-type record now bound.

## Graph, binary and exact weighted contribution

GraphBridges explicitly converts C target-row/source-column matrices to B transposes. Paper boundary matrices are the transposes of B graphOperator, so rectangular norms agree. The input-output equivalence derives sole source/sink conditions from acyclicity and paths. Graph rank preserves the exact distinguished VV^T matrix under transpose using its symmetry. No sum of absolute graph monomials replaces the signed contraction.

GraphModifications proves every fixed boundary entry survives the explicit fiber split, by separating consistent labelings and reindexing them bijectively to original labelings. Selected copies retain both designated boundary labels. Edge reversal/transposition is proved entrywise by the exact edge equivalence. Identity operator norm is exactly one under the source's positive coordinate dimension; the complete fiber norm product follows. This is not merely equality after summing all boundaries. It also does not by itself encode an arbitrary finite modification history.

BinaryBridges proves the two forbidden pair configurations vanish by XOR incompatibility under injective equality-block labels; the source-index sum is explicitly reindexed from B's finite block enumeration. The unrestricted XOR count uses the full actual constraint-matrix rank and equals n^(v-h), with injective solutions included as a subtype. The admissibility equivalence identifies the three surviving pair cases exactly. Positive block cardinality proves d<=p-1 when needed. The odd-incidence estimate then uses the proved rank-support theorem, retaining actual binary rank rather than a surrogate.

EntryBridges handles all d,h. A nonempty fixed class yields both d<p and s+h<=p before any natural-number subtraction is interpreted algebraically. An empty class has literal sum zero. The weighted count is the actual sum of c(rho), not an RHS definition. The fixed-class contribution has the absolute value of the selector coefficient and absolute values inside both partition and injective-label sums. Nonzero contribution implies XOR support; unrestricted label counting and the exact cumulant bound give the dimension factors, followed by the proved signed-exponent identity and weighted aggregation. This matches C Proposition 10's stronger per-class clause, rather than substituting the already aggregated trace estimate.

## Boundaries retained

I-V6-10 is the full general bridgeless allowed-modification statement, not B's positive-even specialization. The published source independently verifies it. Both a general operator-consequence expected proposition and a separate actual finite-modification-history expected proposition have been independently approved. Primitive fresh-ear selection and vertex-simple return-path helpers have been read and compiled; neither full endpoint theorem has yet been produced. No partial helper is promoted to full conversion. I-V6-14 formal-log squarefree coefficients and I-V6-42 continuous uniform order statistics are written routes independently reviewed; the Lean algebraic cancellation and finite coupling are alternative proofs of their required consequences, not implementations of those literal formal-series/continuous objects. The broader B disjoint weight-four support is a safe superset of C's disjoint-pair support, not identical to it.

No mathematical repair of frozen C is established by the reviewed source and bodies. Final kernel status, forbidden-axiom closure, semantic regressions, detached cold build and replay remain separate evidence and must not be inferred from this semantic reading.

## Independent executable expected-type assignments

Reviewer-owned ExpectedChecks.lean contains 33 examples: 7 full baseline named source types and 26 additive/client checks. For the additives, the left-hand sides use separate expected Props frozen and reviewed before their implementation; the right-hand sides use the actual proof constants. The baseline checks instantiate the earlier frozen C inventory statements, with the explicit universal constant identity retained as a harmless stronger conclusion. The checks do not compare actual-type output to itself and do not edit any expected scope to fit a proof.

The checks include all six cumulant clauses plus their conjunction; graph orientation, fixed boundary sum, operator bound, rank contraction, split/reversal preservation, positive-dimensional identity norm and norm product; all three binary clauses; both all-d,h contribution clauses; arbitrary averaged sampling, generic finite-noise law and concrete joint realization; fixed-frame spectral/squared-event/success clients; and the seven remaining baseline named routes. There is deliberately no example claiming an unavailable literal general-bridgeless modification theorem. Exact check-file and preapproved expected-file hashes are in independent_expected_check_manifest.json. The controller compiled this module in expected_and_graph_iteration5 with returncode 0. Current actual types explicitly reference the preapproved expected definitions, all 53 baseline type expressions are byte-identical to the earlier reviewed baseline manifest, and all 117 files in the current combined source manifest match their recorded hashes. The locks below bind the reviewed current compiler output.

## Current byte binding and limits

reviewed_type_lock.json records each of the 189 current actual theorem type expressions; reviewed_definition_lock.json records the 39 current actual definition types and bodies. These are downstream locks after independent source/type review, not a substitute for that review. Independent expected-type assignments and their preproof expected-file hashes are the separate semantic evidence. The additional primitive graph ear/path helpers and all 17 baseline extraction/graph regression theorem bodies were read; they are sanity or partial-construction results, not extra named C results. SemanticWitnesses was reviewed as a definition-level sanity suite; its Dirac measure on R has an uncountable sample type but finite support, so it is not represented as a non-finite-support law test.

Exact manifest/log/check hashes and reviewed source hashes are recorded in independent_correspondence_binding.json. Any change to those actual types, expected bodies or proof sources requires a delta review. A later addition of the general modification target must preserve the I-V6-10 original scope and receive separate actual-proof review.

Incremental review: the new DAGSplit, EarInsertion and TotalRank proof bodies were read completely. The 19 additional exported theorem types include three previously omitted whitespace-prefixed declarations and 16 local graph construction helpers. The former 170 type expressions and all 39 definition records are unchanged. The current source manifest has 117 imported local files, all hash-verified. Coverage remains 47/50 literal source interfaces and 49/50 required consequences, with I-V6-10 open; I-V6-11 is covered at its source per-operation scope. GeneralBridgelessState has a separately approved partial-invariant definition, but no state transition or initial-state proof has been pre-accepted.
