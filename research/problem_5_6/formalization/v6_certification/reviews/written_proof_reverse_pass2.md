# Independent written-proof reverse pass 2

Reviewer `/root/v6_inventory`, a different context from `/root/baseline_audit` pass 1 and from the proof generators. This is a source-first reverse review. It is independent in context; it is not a fabricated claim that all proof lines were independently reproved twice. The current combined actual-type and source-manifest binding is complete for 189 theorem targets and 39 expected/primitive definition records. ExpectedChecks compiled 33 independent source-scope assignments; general conversion completion remains open.

The first bounded full source read and exact A/B/C candidate map appear in written_proof_source_review.md. This second pass returns from the actual current implementations to C's written claims, especially the mismatches that required new bridges. It reads every actual additive proof body currently listed in independent_forward_correspondence.md and reconciles the source inventories without changing their denominators. Baseline pass 1 supplies a separate reviewed traversal of the actual main, graph and trace proof body routes; that report is evidence with its own bounds, not this reviewer's claimed duplicate body inspection.

## Reverse checks

1. Main result and interpretation: the actual result retains the universal constant, deterministic k before the frame, the supremum outside probability, exact uniform sign/sign/subset law and n=2^m including m=0. The additive fixed-frame and squared-norm clients reconstruct the written OSE interpretation. A good spectral event controls every vector in this one frame. The written three-width arithmetic and full-sample exact branches remain consistent with the B endpoint.

2. Transfer, signed trace and Bernoulli consequence: C's pointwise compensation retains arbitrary delta and orthogonal projections; delta=r/n is imposed only in the randomized application. The signed trace bound has abs(E tr), not E abs(tr). The two-projection endpoint cases, complement contribution and defective-root trace recurrence were checked against the written expanded derivation and B's actual theorem types. No numerical or finite sample experiment replaces these inequalities.

3. Cumulants: the source's scalar variables with requisite finite joint moments now have six genuine measure-space propositions. The proofs' occurrence-position semantics permit repeated random variables. The written formal-log route is mathematically valid as a squarefree formal computation and does not need analytic moment-generating convergence; Lean proves its needed identities by partition cancellation. Independent-family vanishing, shifts and odd symmetry are actual theorem consequences with their correct hypotheses, rather than undefined primitive facts.

4. Graphs: transposition resolves the manuscript/B row convention. Loops and parallel edge occurrences remain explicit, the sum is signed and unrestricted, and the projection factorization inserts two r-dimensional boundaries. Boundary-preserving fiber split and reversal now establish the stronger entrywise equalities. The general conversion prose remains broader than the even Euler route. The exact published Mingo source was read: Theorem 11(1), p.2281; Definition 13 and Proposition 14, p.2285; Lemma 16, p.2286. The published proof starts from a simple boundary path and successively adjoins ears, splitting when the ear returns to the same vertex. Thus the earlier preprint/journal numbering uncertainty is resolved. This is external-source verification, not an imported Lean theorem or proof of modification history.

5. Equality/entry counting: C distinguishes selector equality pi from entry cumulant rho, and both are retained. The p=2 and p=7 examples and contraction figure are source-checked in the first complete read. The actual binary bridge uses rank of every constraint row; its injective bound is a subset bound. The forbidden cases require injectivity, and XOR is a necessary rather than sufficient condition for nonvanishing. The actual entry contribution keeps the exact fixed class and internal absolute values, with empty d,h classes handled before negative-power normalization. The support used in B may contain additional weight-four rows from large blocks; it is deliberately a superset at the counting step, so the written disjoint-pair U is not definitionally identified with it.

6. Sampling and small ranks: the source continuous common-order construction is valid, with ties null, exact marginal distributions and independence from X. The formal finite common-order coupling is an alternative route. Actual arbitrary-space pointwise integration removes the finite-X restriction, and the actual joint-realization client now transports the exact three finite sampling marginals and independence assumptions into that averaged theorem. The current compiled expected assignment and actual-type record now bind that application. The Loewner spectral sandwich and binomial denominator 48 are preserved. Conditional finite-population second moment, corrected Rademacher fourth moment and Markov inequality give the stated small-rank bound; exact n=1/full-sample treatment prevents division-by-zero use.

## Current outcome

NECESSARY_WRITTEN_MATHEMATICAL_REPAIR_FOUND: NONE_IN_THIS_REVIEW.

SOURCE_ROUTE_INDEPENDENTLY_REVIEWED: YES, with full frozen scope retained.

WHOLE_C_LITERAL_KERNEL_COVERAGE: NOT_ASSERTED. The three distinctions are (a) literal general modification history, (b) formal-log/continuous route objects versus alternative proofs, and (c) whole-source versus current compiled consequence coverage. The concrete sampling-law client and current combined manifest have now been bound. Source review, named consequence proof, transitive kernel evidence and publication authority remain different conclusions.
