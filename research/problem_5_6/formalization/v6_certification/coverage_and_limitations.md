# Coverage and limits of the frozen-v6 companion

The target is manuscript commit `4551d08f3732a3ca06c2f576794e1d75790e1972`.
Full manuscript certification is **not established**: interface I-V6-11 needs
an explicit convention that generic vertex coordinate dimensions are positive.
The proposed clarification in `required_manuscript_repairs.md` is not applied.
The named graph bound and main embedding theorem are not refuted by this issue.

The frozen inventory contains 12 named results with 30 conjuncts, 50 additional
theorem-strength interfaces, and 34 definitions or notation objects. Independent
correspondence accepts all 12 named results, 49 of the 50 interfaces at their
literal scope, and the 34 definition mappings, with the dimension ambiguity
localized to I-V6-11. The 96 rows have 58 exact reuses, 25 wrapper-only entries,
12 new formalizations, and one source clarification. These counts describe
source rows; helper declarations do not enlarge the paper denominator.

The combined audit checks 359 theorem targets, including all 53 historical B
targets and the new implementation/client/regression targets. It also records
134 raw definitions and 63,329 conservative dependency nodes. The source manifest
contains 159 unique files. The historical B operation remains PROBE; this new
operation requests CERTIFICATION against C and reports its remaining limitation.

The additive mathematical work covers real cumulants on arbitrary probability
spaces with the requisite finite joint moments, literal formal-log coefficients,
arbitrary independent random orthonormal frames, continuous uniform sampling
keys, each stated binary-constraint clause and weighted entry sum, graph matrix
orientation and identity-splitting transports, and the trace/centering identities.
The bridgeless graph construction produces a finite permitted modification
history and the resulting input-output graph from graph hypotheses. The analytic
bound is not supplied as a field of the graph or as an assumed certificate.

For arbitrary natural coordinate dimensions, the splitting proof establishes
boundary/full-sum preservation and nonincrease of the edge-norm product. With
positive dimensions it establishes norm-product equality. This does not prove
the manuscript's unrestricted generic identity-norm-one sentence when a
zero-dimensional coordinate space is allowed.

The Mingo--Speicher comparison records a direct adaptation for the consequences
used here. It does not claim the arbitrary rooted-star relation implements all
of their Definition 13 or preserves their full bridge-block forest. Written
proof correspondence was reviewed separately from theorem truth. A new,
source-first reverse reviewer retained I-V6-11 as a necessary clarification.

The frozen TeX was compiled to resolve numbering and references. Pages 4, 10,
11, 12 and 25 were rendered and visually inspected; the entire PDF was not
visually inspected. Bibliographic background and expository diagram consistency
are not labeled as kernel-verified research conclusions.

The mandatory suite distinguishes compilation, expected-type client checks,
semantic witnesses, 19 historical structural guards, 25 invalid-evidence
fixtures, transitive dependency/axiom checks, and fresh stored-proof kernel
replay. Constructor/recursor traversal conservatively overapproximates
`collectAxioms`; both sets are retained and independently checked against
`propext`, `Classical.choice`, and `Quot.sound`. The replay uses Lean's kernel,
not an independently implemented logic checker. The installed standard Lean
toolchain, operating system and hardware remain in the trust boundary.

The initial c030 archive's cold build and fresh replay passed, but its
single-command suite failed because the standalone audit logger was not built.
The unchanged source was built explicitly and the recovered full evidence audit
passed. Both the failure and recovery are retained. The corrected entrypoint
adds the missing audit-module prerequisite without changing mathematical bytes.
The final corrected archive at `6bb96aa` passed its documented command in
one invocation, without recovery after the command began. Its new unified
build, seven mandatory checks, fresh kernel replay, and 359-target audit all
passed; the independent completion review confirmed the actual receipts and
unchanged archive inputs. Exact identities are in `export_candidate.json` and
`reproduction/independent_final_source_cold_completion_6bb96aa-final.json`.
All proof objects were newly compiled. The run reused verified source Git
objects after transport failures, with zero initial/prebuild compiled objects
and no compiled-cache reuse. It is not labeled an unqualified cache-free run.
The full-paper scope gate separately rejected I-V6-11 with exit code 1.

Workflow metadata failures are preserved in `workflow_limitations.md` and the
original validator outputs. Author comprehension, authorship attestations,
novelty, priority, publication readiness and public-release approval are not
asserted. No manuscript, protected B proof/build input, paper branch or main
branch is changed by this operation.
